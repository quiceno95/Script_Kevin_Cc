npx selenium-side-runner  -c "browserName=firefox moz:firefoxOptions.args=['-headless']" "Tets Metdo De Pago.side"

firefox -CreateProfile "selenium /tmp/firefox-selenium-profile"

npx selenium-side-runner -c "browserName=firefox moz:firefoxOptions.args=['-headless','-profile','/tmp/firefox-selenium-profile']" "Tets Metdo De Pago.side"

xvfb-run -a npx selenium-side-runner  -c "browserName=firefox moz:firefoxOptions.args=['-headless','-no-remote']" "Tets Metdo De Pago.side"


1. Instalar Firefox y Geckodriver en Ubuntu
bash
Copiar
Editar
# Instala Firefox
sudo apt update
sudo apt install -y firefox

# Instala geckodriver (driver para Selenium)
sudo apt install -y wget tar
GECKO_VERSION=$(curl -s https://api.github.com/repos/mozilla/geckodriver/releases/latest | grep tag_name | cut -d '"' -f 4)
wget https://github.com/mozilla/geckodriver/releases/download/$GECKO_VERSION/geckodriver-$GECKO_VERSION-linux64.tar.gz
tar -xvzf geckodriver-*.tar.gz
sudo mv geckodriver /usr/local/bin/
sudo chmod +x /usr/local/bin/geckodriver
Verifica:

bash
Copiar
Editar
geckodriver --version
firefox --version
2. Ejecutar tu prueba con Firefox
bash
Copiar
Editar
npx selenium-side-runner -c "browserName=firefox" "Tets Metdo De Pago.side"
Si estás en servidor (sin entorno gráfico)
Firefox también requiere headless:

bash
Copiar
Editar
npx selenium-side-runner -c "browserName=firefox moz:firefoxOptions.args=['-headless']" "Tets Metdo De Pago.side"
